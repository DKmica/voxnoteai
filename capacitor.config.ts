import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.emeraldslothdive',
  appName: 'emerald-sloth-dive',
  webDir: 'dist'
};

export default config;
